#TODO: not connected with the project yet

from tick_exception import TickException

class TickChecker(object):
	u"""this class allows to check if a tick has a proper value"""
	def check(tick):
		if dif not in (-1,0,1):raise TickException()
	pass
